
modLoader="javafml"
loaderVersion="[44,)"
license="All rights reserved"
[[mods]]
modId="legendfoxes"
version="1.0"
displayName="Minecraft Legends Foxes"
logoFile="banner.png"
credits=""
authors="Cosmo, Bretzelfresser"
description='''
Legend Foxes is a mod that replaces all the Minecraft Legends piglins into fox-like mobs, however some can be changed.'''
[[dependencies.legendfoxes]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[44,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.legendfoxes]]
    modId="minecraft"
    mandatory=true
# This version range declares a minimum of the current minecraft version up to but not including the next major version
    versionRange="[1.19.4,1.20)"
    ordering="NONE"
    side="BOTH"
